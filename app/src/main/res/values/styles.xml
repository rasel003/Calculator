<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/arabic_green</item>
        <item name="android:statusBarColor">@color/arabic_green</item>
        <item name="android:navigationBarColor">@color/arabic_green</item>
    </style>

    <style name="ShapeAppearance.App.Circular" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>
